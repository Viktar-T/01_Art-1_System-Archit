\firstpage{1} 
\makeatletter 
\setcounter{page}{\@firstpage} 
\makeatother
\pubvolume{1}
\issuenum{1}
\articlenumber{0}
\pubyear{$if(year)$$year$$else$\the\year{}$endif$}
\copyrightyear{$if(year)$$year$$else$\the\year{}$endif$}
%\externaleditor{Academic Editor: Firstname Lastname}
\datereceived{ } 
\daterevised{ } % Comment out if no revised date
\dateaccepted{ } 
\datepublished{ } 
%\datecorrected{} % For corrected papers: "Corrected: XXX" date in the original paper.
%\dateretracted{} % For corrected papers: "Retracted: XXX" date in the original paper.
\hreflink{https://doi.org/} % If needed use \linebreak
%\doinum{}
%\pdfoutput=1 % Uncommented for upload to arXiv.org
%\CorrStatement{yes}  % For updates

\Title{$if(title)$$title$$endif$}

% MDPI internal command: Title for citation in the left column
\TitleCitation{$if(runningtitle)$$runningtitle$$else$$title$$endif$}

% Author Orchid ID: enter ID or remove command
$for(by-author/pairs)$
$if(it.value.orcid)$
\newcommand{\orcidauthor$it.key/alpha/uppercase$}{$it.value.orcid$} 
$endif$
$endfor$

% Authors, for the paper (add full first names)
\Author{$for(by-author)$$it.name.literal$$if(it.affiliations)$\textsuperscript{$for(it.affiliations)$$it.id$$sep$,$endfor$}$endif$$sep$, $endfor$}

% MDPI internal command: Authors, for metadata in PDF
\AuthorNames{$for(by-author)$$it.name.literal$$sep$, $endfor$}

% MDPI internal command: Authors, for citation in the left column
\newcommand\getfirst[1]{\getfirstaux#1\relax} \def\getfirstaux#1#2\relax{#1}
\newcommand{\authorformatted}{$for(by-author)$$it.name.family$, \getfirst{$it.name.given$}.$sep$; $endfor$}
\AuthorCitation{\authorformatted}

% Affiliations / Addresses (Add [1] after \address if there is only one affiliation.)
\address[$for(by-affiliation)$$it.id$$sep$,$endfor$]{%
$for(by-affiliation)$
\textsuperscript{$it.id$} \quad $it.department$, $it.name$\\
$endfor$}

% Contact information of the corresponding author
$for(by-author)$
$if(it.attributes.corresponding)$
\corres{Correspondence: $it.email$}
$endif$
$endfor$

% Abstract (Do not insert blank lines, i.e. \\) 
\abstract{$if(abstract)$$abstract$$endif$}

% Keywords
$if(keywords)$
\keyword{$for(keywords)$$keywords$$sep$; $endfor$}
$endif$
