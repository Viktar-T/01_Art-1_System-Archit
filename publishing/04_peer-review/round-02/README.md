# round-02/ — placeholder

If round 1 returns a Minor or Major Revision decision again, mirror the structure of `../round-01/` here:

```bash
cd publishing/04_peer-review
cp -r round-01 round-02
# then clear out the content of each file, keeping the headings/structure.
```

Then follow the workflow in `../README.md`.

If round 1 ends in **Accept**, leave this folder empty (or delete it).
